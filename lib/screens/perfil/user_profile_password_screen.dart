
import 'package:flutter/material.dart';

class UserProfilePasswordScreen extends StatelessWidget {
  const UserProfilePasswordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Perfil Password')),
      body: const Center(child: Text('Tela Perfil Password')),
    );
  }
}
