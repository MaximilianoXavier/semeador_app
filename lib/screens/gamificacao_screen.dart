import 'package:flutter/material.dart';

class GamificacaoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gamificacao'),
        backgroundColor: Color(0xFF143D2B),
      ),
      body: const Center(
        child: Text(
          'GamificacaoScreen (em construção)',
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}
