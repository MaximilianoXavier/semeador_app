
import 'package:flutter/material.dart';

class TerraProgressScreen extends StatelessWidget {
  const TerraProgressScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Minha Terra ProgressScreen'),
      ),
      body: const Center(
        child: Text('Tela TerraProgressScreen em construção'),
      ),
    );
  }
}
